# Screenshots

Captures of the four main views, used by the top-level README.

Current files:

- `stats.png` — the Stats dashboard
- `manage-transactions.png` — the Transactions tab in Manage
- `manage-rules.png` — the unified Rules page
- `manage-categories.png` — the Categories tab

To refresh: load `samples/sample-backup.json` first
(Manage > Backup / Restore > Import everything) so every view has
realistic-looking data, then re-capture each tab.

Pending: a `landing.png` (Home hub) and `import-review.png` (Import
review table mid-edit) — see the TODO note in the top-level README.
